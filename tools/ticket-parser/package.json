{
  "name": "holy-corner-ticket-parser",
  "version": "0.1.0",
  "private": true,
  "description": "Parse docs/tickets/*.md into the bcap-contracts Ticket contract (HC-001). Ported from fountainbridge's tools/ticket-parser (FB-004). Isolated from the app (HC-002) so it needs no root package.json; tolerant of real ticket variation across holy-corner, grassmarket and fountainbridge.",
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts",
    "parse": "node --import tsx src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
